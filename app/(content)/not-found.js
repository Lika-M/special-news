export default function NewsNotFound(){
    return (
        <div id="error">
            <h1>Not found!</h1>
            <p>Unfortunately, we could not found requested article.</p>
        </div>
    );
}